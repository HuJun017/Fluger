name: totem
description: Totem cliente per la hamburgeria
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  http: ^1.2.0

flutter:
  uses-material-design: true
